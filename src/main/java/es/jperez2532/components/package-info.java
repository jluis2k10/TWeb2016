/**
 * Clases auxiliares que no tienen una función clasificable dentro de los
 * otros paquetes del proyecto.
 * <p>
 * No todas estas clases son <em>componentes</em> en el sentido estricto que
 * define Spring (clases manejadas por el propio Spring), pero no veo necesario
 * para un proyecto de este tamaño inclir más paquetes que contengan únicamente
 * una o dos clases.
 */
package es.jperez2532.components;